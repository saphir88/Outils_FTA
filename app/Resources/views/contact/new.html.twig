{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {% for message in app.flashes('success') %}
            <div class="col-lg-offset-3 col-lg-6 flash-notice alert alert-success">
                <span class="glyphicon glyphicon-ok" ></span> {{ message }}
            </div>
        {% endfor %}
    </div>
    <div class="container">
        <div class="row">
            <div class="contact col-lg-6 col-lg-offset-3">
                <h1>Nous Contacter</h1>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <p>Merci de complèter les champs obligatoires (*).</p>
                <input class="btn btn-success" type="submit" value="Envoyer" />
                {{ form_end(form) }}

            </div>
        </div>
    </div>
{% endblock %}
