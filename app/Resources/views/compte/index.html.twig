{% extends 'base.html.twig' %}

{% block body %}

    {{ dump(utilisateur) }}

    <h1>Modification de la Startup "{{ utilisateur.nomStartup }}"</h1>
    <div class="container">
        {{ form_start(form) }}
        {{ form_row(form.nomStartup) }}
        {{ form_label(form.logo) }}
        <br>
        <img src="{{ asset('uploads/img/') }}{{ utilisateur.logo }}" width="300">
        {{ form_errors(form.logo) }}
        {{ form_widget(form.logo) }}

        {#{{ form_row(utilisateur.password) }}
        #}
        {{ form_rest(form) }}

        <div class="container">
            <button type="submit" class="btn btn-success">Valider les modifications</button>
            {{ form_end(form) }}
            {#{{ form_start(dform) }}
            <button class="btn btn-danger" type="submit">Supprimer</button>
            {{ form_end(delete_form) }}#}
        </div>
        {#<a class="btn btn-primary" href="{{ path('communautes_index') }}">Revenir à la liste des Startups</a>#}
    </div>
{% endblock %}
