
{% extends 'base.html.twig' %}

    {% block body %}
        <div class="container center">
            <h1 style="text-align: center">Changer mon mot de Passe</h1>
            <div class="login col-lg-4 col-lg-offset-4">
                <div class="col-lg-12 ">
                    {{ form_start(form, { 'action': path('fos_user_change_password'), 'attr': { 'class': 'fos_user_change_password' } }) }}
                    {{ form_widget(form) }}
                    <div>
                        <button href="{{ 'change_password.submit'|trans }}"class="btn btn-success" type="submit">Mettre à jour</button>
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>

    {% endblock %}



